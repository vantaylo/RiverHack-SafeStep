import {Vibration} from 'react-native';

export default function crossVibration (timeRemaining: number): void {
    function customVibrate(pauseDuration: number, duration: number): void {

    }
    // Vibrate 2x to signal safe to cross
    // Vibrate slow pulse to signal crossing time
    // Vibrate agressivelly to signal warning
    // Vibrate pulse to say crossing is done
}

